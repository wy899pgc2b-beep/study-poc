// 判定のしきい値など。技術検証ではここの値を調整する(設計書 4 章の初期案に対応)。

// 試作品の版。公開のたびに上げ、version.json も同じ値にする(古い版がキャッシュから読み込まれたことに気づけるように。
// 9 回目:公開の 3 分後に始めた検証が、キャッシュに残った 1 つ前の版で動いていた)
export const APP_VERSION = 'poc-15';
export const DEFAULTS = {
  analysisFps: 5,

  // 離席(設計書 3.10)
  awaySec: 20,
  returnSec: 3,

  // よそ見(設計書 4.4):キャリブレーション時の頭の向きからのずれ
  lookAwayYawDeg: 25,
  lookAwayPitchUpDeg: 20,
  lookAwaySec: 3,

  // 目の閉じ具合(設計書 4.8)
  // 実機検証で調整:3 回目 0.3 → 0.15、0.45 → 0.6。4 回目は顔を上げて目を閉じても閉じ具合 0.64(基準 0.55)・EAR 比 0.58 で、
  // 境目でちらついたため 0.07・0.8 にした。
  // 7 回目(横向きに立てかける):読む・書くときの EAR 比が 0.59〜0.75 まで下がり(閉じ具合は 0.44 以下)、居眠りと誤判定した。
  // 目を閉じたときは EAR 比 0.20〜0.29・閉じ具合 0.58 以上。EAR 比だけで閉眼とするのは 0.5 未満に限り、
  // 0.5〜0.8 は閉じ具合もやや高いときだけ閉眼とする
  blinkMarginOverCal: 0.07, // キャリブレーション時の閉じ具合 + この値 を「閉じている」とみなす
  // 自由に学習(横向き・バックカメラ。8 回目の前)で、書いている最中に居眠りと誤判定した。2〜7 回目の読む・書くときの閉じ具合は
  // 90% の値で 0.53 以下、目を閉じたときは 10% の値で 0.58 以上なので、その間の 0.55 にした(以前は 0.45)。
  // 平置き(5 回目)は目を閉じても 0.36〜0.58 と低かったが、EAR 比が 0.5 未満なので目の形だけで閉眼と判定できる
  blinkMin: 0.55,
  blinkMax: 0.8,
  earRatioWithBlink: 0.95, // 表情係数が高く、かつ EAR 比がこの値未満なら閉眼
  earRatioAlone: 0.5, // EAR 比がこの値未満なら表情係数に関係なく閉眼
  earRatioStrong: 0.8, // EAR 比がこの値未満で、表情係数もやや高い(下の 2 つ)なら閉眼
  blinkSlackWithEar: 0.1, // …「やや高い」= 閉眼の基準 − この値 以上
  blinkMinWithEar: 0.5, // …かつこの値以上(7 回目:読むときの閉じ具合は 0.44 以下)
  earRatioStrongWhenDown: 0.45, // キャリブレーションより 15° 以上深くうつむいているときの基準
  // 本人の「目を閉じたとき」の基準(キャリブレーションで 3 秒目を閉じてもらう)。目の形と閉じ具合が、開いたとき(0)と
  // 閉じたとき(1)の間のどこにあるかを平均し、この値以上なら閉眼。8 回目(バックカメラ・メガネ)は前に傾いて目を閉じても
  // EAR 比 0.67・閉じ具合 0.35 で、上の基準では判定できなかった(この方法だと 0.57。読む・書くは 2〜8 回目とも 0.42 以下)
  // しきい値 0.5 では、自由に学習(メガネ・バックカメラ)で起きて読み書きしているだけで居眠り 5 回・うとうと 13 回の誤報になった。
  // 10 回目(メガネ):前に傾いて目を閉じると 0.91〜1.05、読む・書くは 90% の値で 0.09 以下。自由学習の誤報の直前 21 場面でも、
  // 中央値が 0.85 以上だったのは 2 場面(どちらも目の形でも閉眼と判定された場面)だけ。そこで 0.85 にし、直近 2 秒の中央値で判定する
  usePersonalClosed: true,
  personalCloseScore: 0.85,
  personalCloseScoreWhenDown: 0.95, // キャリブレーションより 15° 以上深くうつむいているとき
  personalSmoothSec: 2, // 一瞬だけ高くなる値に反応しないよう、この秒数の中央値を使う
  personalMinEarDrop: 0.15, // 閉じたときの EAR がキャリブレーション時よりこの割合以上小さくなければ、目の形は使わない
  personalMinBlinkRise: 0.1, // 閉じたときの閉じ具合がこの値以上大きくならなければ、閉じ具合は使わない
  // キャリブレーションで目を閉じたときの値から、目の状態が読み取りやすいかを判断する(利用者への案内のみ)。
  // メガネあり(自由学習):EAR 比 0.54・閉じ具合 +0.29 → 前に傾いた居眠りを判定できなかった。
  // メガネなし(9 回目):EAR 比 0.24・閉じ具合 +0.40 → 判定できた
  eyeSignalClearEarRatio: 0.4, // 目を閉じたときの EAR 比がこれ未満、または
  eyeSignalClearBlinkRise: 0.35, // 閉じ具合がこれ以上上がれば「はっきり読み取れる」
  closedGapSec: 1, // 閉眼の途切れがこの秒数以内なら、閉じたままとみなす
  lookingDownExtraDeg: 15, // キャリブレーションよりさらに下を向いているときは強い証拠だけで判定
  drowsyClosedSec: 3,
  sleepClosedSec: 10,
  perclosWindowSec: 60,
  perclosDrowsy: 0.3,
  perclosMinObservedSec: 20, // これより短い観測では PERCLOS を使わない
  perclosMinFaceRate: 0.7, // 直近 10 秒の顔の検出率がこれ未満なら PERCLOS を使わない
  wakeOpenSec: 2, // 目を開けた状態がこの秒数続いたら目覚めたとみなす
  faceDownSec: 20, // 設計書の初期案は 30 秒。検証シナリオで確かめられるよう 20 秒で試す
  faceGapSec: 2, // 顔の検出のちらつき(この秒数以内の途切れ)は続いているとみなす
  headLowRatio: 0.5, // 肩からの頭の高さがキャリブレーション時のこの割合未満なら「頭が低い」(伏せている)
  faceRateWindowSec: 10, // 顔の検出率を測る時間

  // うつむき具合と在席(設計書 4.9)
  headDownRatio: 0.85, // 肩からの頭の高さがキャリブレーション時のこの割合未満なら「うつむいている」
  // 4 回目:基準からの増え方は 読む 0.06・前に傾く 0.11・書く 0.16・伏せる 0.19・近づける 0.24、顔を上げる −0.07
  crownBowDelta: 0.1, // 頭頂部の見える割合がキャリブレーション時よりこれ以上増えたら「うつむいている」
  segMinHairFrac: 0.005, // 画面のうち髪がこれ以上映っていれば、顔や上半身が見えなくても頭があるとみなす
  segHairRatio: 0.5, // …ただしキャリブレーション時の髪の面積のこの割合以上
  segPersonRatio: 0.4, // …かつ人の面積がキャリブレーション時のこの割合以上
  coverPersonFrac: 0.85, // 人が画面のこれ以上を占め、
  coverEyeDeskCm: 10, // 顔が見えないか目と机(カメラ)の距離がこれ未満なら、頭がカメラを覆っている(平置きで伏せた)
  // 髪の面積がキャリブレーション時のこの割合未満なら、頭は下がっていない(横や後ろを向いた)とみなし、
  // 上半身の特徴点による「頭が低い」を使わない。4〜7 回目:よそ見 0.30〜0.62、うつむく・伏せる 1.07 以上
  hairShrinkRatio: 0.7,
  // 顔が見えず、髪の面積がキャリブレーション時のこの倍数以上なら、頭が机の上にある(伏せている)。書く動作より優先する
  // (8 回目:伏せている間、手の検出のゆらぎで「書いている」と出て、伏せた居眠りを判定できなかった。
  // 伏せると 4.5〜20 倍、書くときは 2.5 倍以下。顔を近づけると 4.6 倍になるが、そのときは顔が見えている)
  headOnDeskHairRatio: 4,

  // 【試験中】前に傾いた居眠りの候補:うつむいたまま、頭も手もほとんど動かない状態が続く
  headMotionWindowSec: 2,
  dozeHeadStill: 0.05, // 頭の動き(肩幅 / 秒)がこれ未満
  dozeHandStill: 0.1, // 手の動き(顔の幅 / 秒)がこれ未満(止まっている手でも検出のゆらぎで 0.06〜0.08 になる。4 回目)
  dozeShadowSec: 5,

  // 手の動き:速さの単位は、顔の幅を 1 とした 1 秒あたりの移動量
  handWindowSec: 1.5,
  handSmoothing: 0.5, // 手の位置の平滑化(1 に近いほど平滑化が弱い)

  // 書く動作(作業)の判定:机の上の手の速さ(docs/verification.md の 3 回目)
  writeSpeedMin: 0.12,
  penGripPinchMax: 0.35, // 【記録のみ】手の形(ペンを持つ形)の目安

  // 【記録のみ】あくび:口の開き(表情係数 jawOpen)がこの値以上の状態が、この秒数続く
  yawnJawOpen: 0.5,
  yawnSec: 1.5,

  // 通知
  // 「うとうと」の音は、この秒数に 1 回まで(自由に学習で、判定がちらついて 1 秒以内に 2 回鳴った)。
  // 設計書 付録B の初期案 10 分にしたところ、「鳴りにくくなった」と利用者から報告があったため 1 分にした。居眠りのアラームは間隔をあけない
  drowsyBeepMinSec: 60,

  // 癖(設計書 4.7)
  habitTouchSec: 1,
  habitGapSec: 0.5, // 手の検出のちらつき(この秒数以内の途切れ)は触り続けているとみなす
  chinRestSec: 5,
  chinRestMaxSpeed: 0.3, // 頬杖とみなす手の速さの上限
  habitMergeSec: 5,

  // 姿勢(設計書 4.9)
  irisDiameterCm: 1.17,
  cameraFovLongSideDeg: 69,
  // 目と机の距離は本人の基準で判定する:キャリブレーション時の距離よりこの割合以上近づいたら「近すぎ」(決定事項 D-7)
  eyeDeskCloseRatio: 0.25,
  eyeDeskAlertSec: 20,
  // 一般的な目安。本人の基準がこれより近いときに、参考として 1 回だけ案内する
  eyeDeskGuidelineCm: 30,
  headCloseRatio: 0.75, // 顔が取れないとき、肩からの頭の高さがこの割合未満なら「近すぎ」
  slouchRatio: 0.8,
  slouchAlertSec: 60,
  tiltDeg: 15,
  tiltAlertSec: 60,

  // 集中度(設計書 4.5)
  minEvaluableSec: 30,
  drowsyWeight: 40,
  habitPenalty: 3,
  habitPenaltyMax: 15,
  interruptionPenalty: 2,
  interruptionPenaltyMax: 10,
};

// 設置スタイルごとのカメラの上向きの傾き(度)。平置きは真上を向く。横向きに立てかけると 7 回目は 23°
export const SETUP_TILT_DEG = { stand: 0, tilt: 45, landscape: 20, flat: 90 };

// 置き方ごとの目安の角度(カメラが水平より上を向く角度)
// 横向き:7・8 回目と自由学習(20.6〜23°)では手元が映ったが、9 回目(30°)は「書く」で手がほとんど映らなかった
export const TILT_RANGE_DEG = { tilt: { min: 35, max: 55 }, landscape: { min: 10, max: 25 } };
